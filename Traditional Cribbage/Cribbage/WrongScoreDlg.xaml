<ContentDialog
    x:Class="Cribbage.WrongScoreDlg"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cribbage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="HUH"     
    Background="{StaticResource bmCherry}"
    
    PrimaryButtonText="Close"   
    
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">
    
    <ContentDialog.TitleTemplate>
        <DataTemplate>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Cribbage" Foreground="White" FontSize="20" />
            </StackPanel>
        </DataTemplate>
    </ContentDialog.TitleTemplate>
    
    <ContentDialog.Resources>
        <Style x:Key="DlgButtonStyle" TargetType="ContentControl">
            <Setter Property="FontFamily" Value="{ThemeResource PhoneFontFamilyNormal}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="{ThemeResource TextStyleLargeFontSize}" />
            <Setter Property="Margin" Value="{StaticResource PivotPortraitThemePadding}" />
        </Style>
        
    </ContentDialog.Resources>
    <Grid Height="600" Width="500" Background="{StaticResource bmFelt}">
        
    </Grid>
</ContentDialog>
